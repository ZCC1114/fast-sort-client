<UserControl x:Class="FastSort.Client.Windows.Views.BlacklistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="680">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="14" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorder}">
            <DockPanel>
                <StackPanel Orientation="Horizontal">
                    <ComboBox Width="150"
                              SelectedValue="{Binding InputTwo, UpdateSourceTrigger=PropertyChanged}"
                              SelectedValuePath="Tag"
                              Margin="0,0,10,0">
                        <ComboBoxItem Content="我的黑名单" Tag="mine" />
                        <ComboBoxItem Content="全网黑名单" Tag="global" />
                    </ComboBox>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Width="220"
                             Height="34"
                             Margin="0,0,10,0" />
                    <ComboBox Width="130"
                              SelectedValue="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                              SelectedValuePath="Tag"
                              Margin="0,0,10,0">
                        <ComboBoxItem Content="全部等级" Tag="" />
                        <ComboBoxItem Content="LV1" Tag="1" />
                        <ComboBoxItem Content="LV2" Tag="2" />
                        <ComboBoxItem Content="LV3" Tag="3" />
                        <ComboBoxItem Content="LV4" Tag="4" />
                        <ComboBoxItem Content="LV5" Tag="5" />
                    </ComboBox>
                    <ComboBox Width="140"
                              SelectedValue="{Binding InputOne, UpdateSourceTrigger=PropertyChanged}"
                              SelectedValuePath="Tag">
                        <ComboBoxItem Content="全部平台" Tag="" />
                        <ComboBoxItem Content="抖音" Tag="0" />
                        <ComboBoxItem Content="淘宝" Tag="1" />
                        <ComboBoxItem Content="小红书" Tag="2" />
                        <ComboBoxItem Content="微信" Tag="3" />
                        <ComboBoxItem Content="快手" Tag="4" />
                    </ComboBox>
                </StackPanel>
                <Button Content="搜索"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource PrimaryButton}"
                        DockPanel.Dock="Right" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorder}">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Top">
                        <TextBlock Text="黑名单列表"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource FastSortText}" />
                        <Grid Margin="0,12,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="44" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="54" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="序号" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource FastSortMuted}" />
                            <TextBlock Grid.Column="1" Text="黑名单信息" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource FastSortMuted}" />
                            <TextBlock Grid.Column="2" Text="等级" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource FastSortMuted}" HorizontalAlignment="Right" />
                        </Grid>
                    </StackPanel>

                    <ListBox ItemsSource="{Binding PrimaryRows}"
                             SelectedItem="{Binding SelectedRow}"
                             ItemContainerStyle="{StaticResource PlainListBoxItem}"
                             BorderThickness="0"
                             Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource FastSortBackground}"
                                        CornerRadius="10"
                                        Padding="12"
                                        Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="44" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="54" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Id}"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource FastSortMuted}"
                                                   TextTrimming="CharacterEllipsis" />
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource FastSortText}"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Detail}"
                                                       FontSize="12"
                                                       Foreground="{StaticResource FastSortMuted}"
                                                       Margin="0,5,0,0"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Time}"
                                                       FontSize="12"
                                                       Foreground="{StaticResource FastSortMuted}"
                                                       Margin="0,5,0,0"
                                                       TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Meta}"
                                                   Foreground="{StaticResource FastSortAccent}"
                                                   FontWeight="SemiBold"
                                                   HorizontalAlignment="Right" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorder}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="14" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="详情"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource FastSortText}" />
                        <ItemsControl ItemsSource="{Binding Metrics}" Margin="0,14,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="4" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource FastSortBackground}"
                                            CornerRadius="10"
                                            Padding="12"
                                            Margin="0,0,10,0">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}"
                                                       FontSize="12"
                                                       Foreground="{StaticResource FastSortMuted}" />
                                            <TextBlock Text="{Binding Value}"
                                                       FontSize="18"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource FastSortText}"
                                                       Margin="0,5,0,0" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <ListBox Grid.Row="2"
                             ItemsSource="{Binding DetailRows}"
                             SelectedItem="{Binding SelectedRow}"
                             ItemContainerStyle="{StaticResource PlainListBoxItem}"
                             BorderThickness="0"
                             Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource FastSortBackground}"
                                        CornerRadius="10"
                                        Padding="12"
                                        Margin="0,0,0,10">
                                    <StackPanel>
                                        <DockPanel>
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource FastSortText}"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding Amount}"
                                                       DockPanel.Dock="Right"
                                                       Foreground="{StaticResource FastSortAccent}"
                                                       FontWeight="SemiBold" />
                                        </DockPanel>
                                        <TextBlock Text="{Binding Detail}"
                                                   Foreground="{StaticResource FastSortMuted}"
                                                   Margin="0,6,0,0"
                                                   TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding Meta}"
                                                   Foreground="{StaticResource FastSortMuted}"
                                                   Margin="0,6,0,0" />
                                        <TextBlock Text="{Binding Time}"
                                                   FontSize="12"
                                                   Foreground="{StaticResource FastSortMuted}"
                                                   Margin="0,6,0,0" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <DockPanel Grid.Row="3" Margin="0,12,0,0">
                        <TextBlock Text="{Binding StatusText}"
                                   Foreground="{StaticResource FastSortMuted}"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Center" />
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <Button Content="加载详情"
                                    Command="{Binding PrimaryActionCommand}"
                                    Style="{StaticResource PrimaryButton}"
                                    Margin="0,0,8,0" />
                            <Button Content="删除明细"
                                    Command="{Binding SecondaryActionCommand}" />
                        </StackPanel>
                    </DockPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
